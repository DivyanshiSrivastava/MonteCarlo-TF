# MonteCarlo-TF
Interpreting Deep Networks Using MCTS


Deep neural networks (DNNs) are widely used to model genome-wide transcription factor (TF) binding specificity. However, DNNs are challenging to interpret. Here, we propose MCTS-based methods to identify the features driving DNN predictions. 
